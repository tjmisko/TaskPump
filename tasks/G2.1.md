---
id: "G2.1"
phase: "G2"
title: "Reconcile README and CHANGELOG with the flipped defaults"
status: done
claimed_by: null
claimed_at: "2026-08-13T06:07:36Z"
turn_budget_remaining: null
consecutive_failed_iterations: 0
blockers:
  - "G1.8"
completed_by_commits:
  - 018ca67
  - a590a8a
  - c562cdf
milestone: "v0.1.0"
files:
  - "README.md"
  - "CHANGELOG.md"
last_heartbeat_head_sha: null
completed_at: "2026-08-13T06:24:27Z"
goal: "README and CHANGELOG describe the flipped defaults accurately, the pending README intro rewrite is committed, and the Still-Arachne-shaped list contains only what is actually left."
last_heartbeat_ts: null
blocked_at: null
blocked_reason: null
resume_attempts: 0
resume_head_sha: null
---

# G2.1 — Reconcile README and CHANGELOG with the flipped defaults

## Spec

Bring the two consumer-facing documents in line with the flipped tree:

- Commit the pending README intro rewrite (sitting uncommitted in the working
  tree since 2026-08-12), fixing the `superviser` typo and trailing whitespace
  while it lands.
- Update the quickstart: after G1.6 a repo using `tasks/` and `T`-ids needs
  *zero* conf keys, so the "two keys is a working configuration" framing becomes
  "works bare; a conf is how you diverge".
- CHANGELOG: move every flipped default out of "Still Arachne-shaped" into
  Changed (with the old spelling noted, since arachne.conf is now the
  compatibility story), and add the "do not upgrade under a live drain" note
  from G1.3/G1.5. The Still-Arachne-shaped list afterward must contain exactly
  the items G3 exists to fix: prefix-scrape liveness, and branch-slug
  round-tripping.

## Scope

Prose only. The layout table, doc links, and provenance section stay.

## Acceptance

- Every command in the README quickstart runs verbatim against
  `tests/fixtures/generic-project` (or a scratch repo where the quickstart says
  so).
- `rg -n 'arachne' README.md` matches only the Provenance section and the
  examples/arachne.conf reference.
- CHANGELOG's Still-Arachne-shaped list and the G3 phase in this ledger name
  the same items.

## Completion notes (2026-08-13T06:24:27Z)

Pending README intro rewrite landed (superviser typo and trailing whitespace
fixed in the same commit); quickstart reframed to "works bare; a conf is how
you diverge" with a load-bearing mkdir tasks step and a tp task resolve
pointer; CHANGELOG censused the G1.4/G1.6/G1.7 flips into Changed with old
spellings. rg 'arachne' README.md matches only Provenance and the
examples/arachne.conf reference. Every quickstart command ran verbatim
(scratch repo + fixture: ready --count 3, --count-eligible 1, next T2.1).
The primary's uncommitted README dirt was stashed across the merge, verified
byte-subsumed by the agent's commits (only the typo/whitespace/quickstart
deltas remained), and dropped.

Flags recorded for follow-up: (1) Still-Arachne-shaped holds the spec's exact
two items (prefix-scrape liveness, branch-slug round-tripping) — G3.5's gate
credential degradation is NOT in the list; acceptance criterion 3 read
literally is unsatisfiable under the spec's own two-item instruction.
(2) DEFECT hit live: a bare repo WITHOUT tasks/ silently falls back to the
installation root's ledger (libexec/tp-task:71) — the agent's first scratch
test wrote a create into the worktree's own ledger before hard-resetting;
follow-up task authored as G3.6. (3) examples/minimal.conf still opens with
the stale "Two keys" framing — fixed by the supervisor post-merge.
(4) tp task claim does not enforce blocker eligibility (the quickstart's
claim-before-blocker-done sequence works because of this).

12 suites green in the worktree at main's tip; prose-only diff.
