---
id: "G5.2"
phase: "G5"
title: "Re-verify the F80 dry-run equivalence against tagged v0.1.0"
status: done
claimed_by: null
claimed_at: "2026-08-14T01:35:23Z"
turn_budget_remaining: null
consecutive_failed_iterations: 0
blockers:
  - "G2.3"
completed_by_commits: []
milestone: "arachne-f90"
files:
  - "examples/arachne.conf"
last_heartbeat_head_sha: null
completed_at: "2026-08-14T01:50:07Z"
goal: "The F80 dry-run byte-equivalence recorded in Arachne's 2026-08-12 handoff is re-verified against tagged v0.1.0 plus the checked-in arachne conf, and the result is recorded in the handoff's thread."
last_heartbeat_ts: null
blocked_at: null
blocked_reason: null
resume_attempts: 0
resume_head_sha: null
---

# G5.2 — Re-verify the F80 dry-run equivalence against tagged v0.1.0

## Spec

Arachne's 2026-08-12 handoff (`ops/planning/handoffs/2026-08-12 19:17 -
f80-via-taskpump-rebinding.md`) recorded that `tp task ready --phases F80`
matches Arachne's own output and `tp pump --phases F80 --dry-run` is
byte-identical from six config lines — but against the pre-tag working tree.
Re-run the comparison against what F90.4 will actually pin:

- In the Arachne checkout, with `tp` resolved from the tagged v0.1.0 tree and
  the full `examples/arachne.conf` (not the six-line subset) as
  `taskpump.conf`, capture `tp task ready --phases F80` (table and `--json`)
  and `tp pump --phases F80 --dry-run`.
- Byte-diff each against the `scripts/arachne-*` equivalents at Arachne's
  current HEAD.
- Any diff is triaged to a named default flip and either accepted (a pin was
  meant to change it — then arachne.conf or the acceptance here is wrong and
  one of them gets fixed) or fixed before Arachne's F90.5 canary runs.

## Scope

Read-only verification plus a written record. No changes to Arachne beyond the
temporary conf copy; no live pump run — `--dry-run` only.

## Acceptance

- The comparison record (commands, tag sha, Arachne HEAD sha, verdict per
  command) is appended to the handoff's thread in Arachne's ops repo.
- Verdict is byte-identical for all three captures, or every diff line is
  traced to its G1 task and resolved.
- Arachne's F90.4/F90.5 tasks are commented with the result.

## Completion notes (2026-08-14T01:50:07Z)

Re-verified against tagged v0.1.0 (0fbf96f5bdc4) at Arachne HEAD 3aa8d22d, TaskPump main a26fd129 (12 suites / 1097 assertions green).

Leg 1 (v0.1.0 examples/arachne.conf as taskpump.conf, from Arachne repo root): ready table BYTE-IDENTICAL (F80.1/.2/.6/.9); ready --json differs (4 lines, `file` repo-relative vs absolute -> #1, not a G1 flip); pump --dry-run differs by the additive gates: line + cap 6-vs-4 (example's TASKPUMP_JOBS=6 pin vs arachne-pump's hardcoded 4 -> #8); plan body identical.

Leg 2 (same conf, from crates/arachne-schema): live #1 repro — empty frontier, rc=0, no warning. ESCALATION, worse than the issue text: tp pump --phases F80 --dry-run from the subdir prints "DONE F80 (no open tasks)" at rc=0 — a silent false drain of the F79 563-tick class. Until #1 is fixed, every real pump launch (F90.5's canary above all) must be made from the repo root.

Leg 3 (the conf F90.4 will actually pin — drop TASKS_DIR/LEDGER_REPO/CODE_REPO, LEDGER_PROBE anchor, SUBMODULE_PROBE=taskpump/bin/tp, JOBS=4, no PRE_FLIGHT): all three captures BYTE-IDENTICAL from repo root AND subdirectory, modulo the additive gates: line and a live token-gate countdown that scripts/arachne-pump rendered identically seconds later. Every diff line traced and resolved — acceptance met on its second arm; two tracings land on upstream issues (#1, #8) rather than G1 flips.

Record appended to the thread of Arachne ops planning/handoffs/2026-08-12 19:17 - f80-via-taskpump-rebinding.md; F90.4 and F90.5 bodies commented; verified draft conf committed as planning/2026-08-13 - F90.4 Draft taskpump.conf.md (all in arachne-ops commit b38e254, pushed).
