---
id: "G1.7"
phase: "G1"
title: "Drop the Rust and host-hardware defaults"
status: done
claimed_by: null
claimed_at: "2026-08-13T05:09:28Z"
turn_budget_remaining: null
consecutive_failed_iterations: 0
blockers:
  - "G1.2"
completed_by_commits:
  - 7adca67
  - 9221efa
  - ae5ec6d
milestone: "v0.1.0"
files:
  - "libexec/tp-pump"
  - "libexec/tp-cleanup"
  - "templates/phase-drain-brief.md"
  - "gates/net-health"
  - "docs/GATES.md"
last_heartbeat_head_sha: null
completed_at: "2026-08-13T05:55:44Z"
goal: "No shipped default assumes Rust or this host: verify commands default to empty, disk reclaim runs only when configured, and the net-health gate is off unless a consumer enables it."
last_heartbeat_ts: null
blocked_at: null
blocked_reason: null
resume_attempts: 0
resume_head_sha: null
---

# G1.7 — Drop the Rust and host-hardware defaults

## Spec

Remove the assumptions that the driven project is Rust and the host is this
Apple-Silicon machine:

1. **Verify commands** — `TASKPUMP_VERIFY_CMDS` defaults to
   `cargo fmt --all` / `cargo clippy ... -D warnings`. New default: empty. The
   phase-drain brief template must render cleanly with an empty verify block
   (no dangling "leave these green:" heading over nothing) — extend the
   renderer-contract tests for the empty case.
2. **Disk reclaim** — correction from G1.1's audit: `TASKPUMP_RECLAIM_CMD`'s
   default is already empty (`tp-pump:178`); the cargo behavior is the
   *built-in empty-case fallback* in the reclaim path, not a default string.
   The flip therefore changes built-in behavior: when the key is unset, the
   per-tick reclaim pass becomes a no-op that logs one line saying it is
   unconfigured, instead of falling back to cargo. `tp-cleanup`'s
   cargo-specific `target/` logic runs only when a reclaim command is
   configured; its `--include-primary` comment naming Arachne is rewritten
   while there.
3. **net-health gate** — `TASKPUMP_HEALTH_GATE` default `1` → `0`. The gate
   stays shipped (its detection logic is a worked example of a real gate) but is
   opt-in; `docs/GATES.md` moves it from "the default chain" to "shipped,
   consumer-enabled", keeping the brcmfmac incident story. The default chain
   becomes token-fresh → usage → disk-low.

`examples/arachne.conf` already pins all three ways Arachne wants them
(G1.1 verifies: `TASKPUMP_VERIFY_CMDS`, `TASKPUMP_RECLAIM_CMD='cargo clean'`,
`TASKPUMP_HEALTH_GATE=1`).

## Scope

Defaults, template empty-case rendering, gate-chain composition, GATES.md. The
gate executables themselves do not change behavior.

## Acceptance

- A brief rendered with no verify commands contains no verify section, asserted
  by the renderer-contract suite.
- `tp pump --dry-run` in a scratch repo shows the three-gate default chain;
  with arachne.conf pins it shows all four, net-health first.
- Reclaim pass with `TASKPUMP_RECLAIM_CMD` unset touches nothing and logs the
  unconfigured line (cleanup suite).
- `./tests/run-all.sh` green; golden suite under pins unchanged.

## Completion notes (2026-08-13T05:55:44Z)

TASKPUMP_VERIFY_CMDS default now empty, delivered via a general renderer
feature: apl_render_template gained conditional {{#KEY}}/{{/KEY}} sections
rendering only when KEY is non-empty; both shipped templates wrap verify prose
in them (the resume note's Rust-flavored prose went generic in the same
stroke). Reclaim: unset TASKPUMP_RECLAIM_CMD makes the per-tick pass a no-op
logging an unconfigured line once per run (not per tick — a 30s-tick drain
would log ~2900x/day) and tp-cleanup --targets skips with its own line, once
per invocation. net-health: TASKPUMP_HEALTH_GATE default 1 -> 0; the pump
never actually read that key before (hardcoded 1, flag-only — the arachne pin
was inert for chain composition until this wired it); GATES.md moves
net-health to shipped-consumer-enabled, keeping the brcmfmac story; default
chain token-fresh -> usage -> disk-low.

Spec conflict resolved: --dry-run had no gate-chain display, so "shows the
three-gate chain" forced a new plan line — unsatisfiable alongside
"golden suite unchanged" byte-for-byte. Goldens re-recorded with exactly ONE
added line each (gates: net-health -> claude-token-fresh -> claude-usage ->
disk-low), supervisor-verified; the golden harness's --no-health-gate hatch
replaced with an inert TASKPUMP_HEALTH_PROBE_CMD=true seam so the pinned
goldens now genuinely pin the four-gate chain. Custom-TASKPUMP_GATES
consumers listing net-health also need TASKPUMP_HEALTH_GATE=1 (documented).
Residual cargo-isms kept per scope: the CONFIGURED reclaim path still guards
on <worktree>/target and tp-cleanup's configured sweep keeps its body.

Agent self-rebased onto post-G1.6 main (one tp-pump comment conflict, G1.6's
favor); 12 suites green post-rebase (cleanup 30, pump 245, golden 37 under
pins).
