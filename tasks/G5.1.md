---
id: "G5.1"
phase: "G5"
title: "Draft the F90 addenda for Arachne's ops ledger"
status: done
claimed_by: null
claimed_at: "2026-08-13T03:09:40Z"
turn_budget_remaining: null
consecutive_failed_iterations: 0
blockers: []
completed_by_commits:
  - 5a59d122ef55a2785f6546c87a1b0d8815c33951
milestone: "arachne-f90"
files:
  - "docs/design/f90-addenda.md"
last_heartbeat_head_sha: null
completed_at: "2026-08-13T03:17:05Z"
goal: "The two Arachne-side gaps outside F90's current scope — repointing the live host services and updating the prep-long-session skill — exist as ready-to-file task texts for the ops ledger."
last_heartbeat_ts: null
blocked_at: null
blocked_reason: null
resume_attempts: 0
resume_head_sha: null
---

# G5.1 — Draft the F90 addenda for Arachne's ops ledger

## Spec

Two Arachne-side gaps surfaced by the 2026-08-12 exploration sit outside F90's
current task set. Write both as create-ready task texts (id, title, goal,
blockers, acceptance) in `docs/design/f90-addenda.md`, for filing into
Arachne's ops ledger with `arachne-task create`:

**F90.8 — Repoint the live host services.** `arachne-disk-guard.timer` is an
active systemd user timer execing `scripts/arachne-cleanup` roughly every five
minutes, outside any drain window, and `arachne-switchboard-recorder.service`
polls docker for containers by the `arachne-agent-` prefix. After F90.4 the
timer must exec the pinned taskpump checkout's `tp cleanup`, the prefix must
stay pinned in Arachne's `taskpump.conf` (the recorder's match contract), and
the swap must happen outside a drain window. Blockers: F90.4. Acceptance:
`systemctl --user cat` shows the new ExecStart; one full timer firing observed
clean in the journal; the recorder still attaches to a test container.

**F90.9 — Update the prep-long-session skill.** The skill's `allowed-tools`
frontmatter grants literal prefixes (`Bash(./scripts/arachne-pump
--dry-run*)`, `arachne-task list*`, `arachne-usage*`,
`run-parallel.sh --list`), and its orient step hard-gates on those scripts
existing — so the *permission grants*, not just paths, break on a clean
cutover, and its "path B" references (`run-parallel.sh`,
`parallel-manifest.tsv`) name files F90.1 deletes. Update grants and gate for
the shim paths now, with a note to re-point at bare `tp` after F90.7.
Blockers: F90.4 (shims exist); the tp-native re-point blocked on F90.7.
Acceptance: the skill runs end-to-end in a post-F90.4 checkout without a
permission prompt; no reference to deleted files remains.

## Scope

Drafting only, in this repository. Filing into ops happens from the Arachne
side by the operator (or an agent working F90 there) — this task's deliverable
is text.

## Acceptance

- `docs/design/f90-addenda.md` exists with both texts in create-ready form,
  each with id, title, one-sentence goal, blockers, and checkable acceptance.
- The file notes its own retirement: once both are filed in ops, the addenda
  doc is deleted here.

## Completion notes (2026-08-13T03:17:05Z)

Worked by session subagent on feat/g5-1, merged to main. Verified: one commit,
docs/design/f90-addenda.md only. Two create-ready texts (F90.8 host-services
repoint, F90.9 prep-long-session skill update), grant literals quoted from the
actual SKILL.md (found five more grants than the spec listed).

Correction to this task's own Spec, discovered during verification: the
disk-guard timer does NOT exec scripts/arachne-cleanup directly. The chain is
timer -> arachne-disk-guard --check -> delegates reclaim to arachne-cleanup.
The addenda encode the real chain. F90.9's blockers were filed as F90.4 only;
the F90.7-gated re-point is an in-body constraint, not a task blocker, so the
shim-path update cannot be wrongly stalled.
