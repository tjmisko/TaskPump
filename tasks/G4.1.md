---
id: "G4.1"
phase: "G4"
title: "tp init scaffolds a new consumer"
status: done
claimed_by: null
claimed_at: "2026-08-14T02:08:30Z"
turn_budget_remaining: null
consecutive_failed_iterations: 0
blockers:
  - "G2.3"
completed_by_commits:
  - "3220026"
  - 7afbc9b
  - b1d1e51
milestone: "v0.2.0"
files:
  - "bin/tp"
  - "libexec/tp-init"
  - "docs/CONFIG.md"
last_heartbeat_head_sha: null
completed_at: "2026-08-14T02:30:06Z"
goal: "tp init scaffolds a working taskpump.conf and tasks directory in any repository, idempotently, and refuses to clobber existing state."
last_heartbeat_ts: null
blocked_at: null
blocked_reason: null
resume_attempts: 0
resume_head_sha: null
---

# G4.1 — tp init scaffolds a new consumer

## Spec

`tp init` — the first command a new consumer runs:

- Writes a commented `taskpump.conf` at the enclosing worktree root, containing
  only the keys a starter actually decides (`TASKPUMP_TASKS_DIR`,
  `TASKPUMP_ID_PATTERN`, and a commented `TASKPUMP_BUILD_GATE`), with a pointer
  to `taskpump.conf.example` for the census. `--tasks-dir <d>` overrides the
  default `tasks`.
- Creates the tasks directory.
- Prints the next three commands (`tp task create …`, `tp task ready`,
  `tp pump --dry-run`) so the quickstart continues from the terminal.
- Refuses to run if a `taskpump.conf` already exists anywhere the discovery
  walk would find one, printing that path — init must never shadow an existing
  ledger's configuration. Re-running after a refusal changes nothing
  (idempotent failure).
- New `libexec/tp-init`, dispatched from `bin/tp`, listed in `tp help`.

## Scope

Scaffolding only. No git commits (the consumer decides when to commit the
conf), no task creation, no fsck (G4.2).

## Acceptance

- Hermetic suite: init in a scratch repo → `tp task create T1 …` →
  `tp task ready` works end to end with no further configuration.
- Second `init` exits non-zero naming the existing conf; the tree is
  unchanged (asserted by diff).
- `init` from a subdirectory writes the conf at the worktree root, not `$PWD`.
- CONFIG.md documents init as the recommended starting point.

## Completion notes (2026-08-14T02:30:06Z)

tp init lands via PR #23 (merge commit c071a324, CI shellcheck+tests green). New libexec/tp-init dispatched from bin/tp ("Getting started" help group); writes a starter taskpump.conf at the enclosing worktree root (subdirectory-safe) carrying only TASKPUMP_TASKS_DIR, TASKPUMP_ID_PATTERN, and a commented TASKPUMP_BUILD_GATE with a pointer to taskpump.conf.example; --tasks-dir override; creates the tasks dir; prints the three-command continuation (tp pump printed WITH --phases T1, deviating from the task text so the printed line runs verbatim). Refusal uses lib/config.sh's own discovery (TP_CONFIG_FILE after tp_load_config) plus a floor at the target path under TASKPUMP_NO_CONF=1; refusal writes nothing and is idempotent. tests/test-tp-init.sh: 64 assertions incl. end-to-end init->create->ready, diff-asserted unchanged tree on refusal, subdirectory and linked-worktree placement. Full suite 16 suites / 1299 assertions green x3; shellcheck -S warning clean.

SPEC_GAP recorded in libexec/tp-init: the generated tasks-dir value is anchored to the conf's own directory (dogfood taskpump.conf:25 construct) rather than bare-relative, because a bare value resolves against $PWD (issue #1, live-reproduced in G5.2 as a silent false drain). To be simplified to the documented bare form once the in-flight config-resolution fix for #1 lands (coordinated with the drain session; noted on PR #23).

Duplicate-claim note: a second G4.1 claim (66e6436, turns=50) came from the parallel drain session's workflow; its duplicate PR #25 was closed unmerged by that session. #23 is authoritative.
