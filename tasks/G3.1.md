---
id: "G3.1"
phase: "G3"
title: "Add the runner.sh list verb to the runner contract"
status: done
claimed_by: null
claimed_at: "2026-08-13T22:12:29Z"
turn_budget_remaining: null
consecutive_failed_iterations: 0
blockers:
  - "G2.3"
completed_by_commits:
  - ff0ff81
  - 4b6403d
milestone: "v0.2.0"
files:
  - "docs/RUNNERS.md"
  - "runners/claude-docker/runner.sh"
  - "tests/test-tp-runner-stop.sh"
last_heartbeat_head_sha: null
completed_at: "2026-08-13T22:28:31Z"
goal: "The runner contract gains a list verb, the reference claude-docker runner implements it, and RUNNERS.md documents liveness as askable rather than docker-name-scraped."
last_heartbeat_ts: "2026-08-13T22:12:33Z"
blocked_at: null
blocked_reason: null
resume_attempts: 0
resume_head_sha: null
---

# G3.1 — Add the runner.sh list verb to the runner contract

## Spec

The v2 addition RUNNERS.md §1.3 promises. Contract:

- `runner.sh list` prints the name of every live agent this runner knows about,
  one per line, and exits 0. An empty list is exit 0 with no output.
- It reads only the configuration `stop` already reads (no per-launch env) —
  the pump calls it once per tick with no task in hand.
- Names printed must be the same names `launch` created (`<prefix><slug>`), so
  existing name→branch mapping keeps working.
- Failure to enumerate (runtime unreachable) exits non-zero with one line on
  stderr — the caller decides the fallback, the runner never guesses.

Implement in `runners/claude-docker/runner.sh` via the existing
`docker ps --filter name=<prefix>` machinery (share the code path with
`lib/pump-lib.sh`'s enumeration rather than duplicating the filter). Rewrite
RUNNERS.md §1.3: liveness is askable in v2; the prefix-scrape remains the
documented fallback and the naming contract stays mandatory.

## Scope

Contract text, reference implementation, tests. The *pump* does not start
calling `list` here — that is G3.2, so this lands independently verifiable.

## Acceptance

- `tests/test-tp-runner-stop.sh` (or a sibling suite) drives `list` against a
  stubbed docker: populated, empty, and unreachable-runtime cases, asserting
  the exit codes and output shape above.
- RUNNERS.md documents the verb, the empty-list case, and the failure case.
- Version note: MINOR bump material, recorded in CHANGELOG Unreleased.

## Released (2026-08-13T21:48:36Z)

Auto-reclaimed by pump: container for feat/g3 is dead and the branch has 0 commits (D3 liveness reclaim).

## Completion notes (2026-08-13T22:28:31Z)

Runner contract v2: `runner.sh list`.

What landed
- lib/pump-lib.sh: apl__runtime_ps_names factors out the single `docker ps
  --filter name=` expression; apl_live_agent_names_strict is the same
  enumeration with the runtime's exit status and stderr propagated instead of
  flattened. Tolerant apl_live_agent_names is unchanged (the pump's tick still
  wants "unreachable reads as none").
- runners/claude-docker/runner.sh: `list` verb, answering from pump-lib rather
  than a second filter. Empty fleet = exit 0, zero bytes. Unreachable runtime =
  non-zero, exactly one line on stderr (docker's first line, the rest dropped).
  Its only input is the prefix (TP_AGENT_PREFIX / TASKPUMP_AGENT_PREFIX /
  ARACHNE_AGENT_PREFIX, default tp-agent-) — no per-launch env. CONTRACT_VERSION
  1 -> 2.
- docs/RUNNERS.md §1.3 rewritten from "what v1 leaves out" to the `list` spec
  (inputs, output, the three-row exit table), plus an explicit subsection saying
  the prefix scrape remains the fallback and the naming contract stays
  mandatory. §1 heading is now v2; the §3 sketch grows a `list` arm.
- runners/claude-docker/README.md: `list` section replacing "Not in v1:
  liveness".
- CHANGELOG Unreleased: MINOR (additive; v1 runners keep working). Fixed the
  0.1.0 entry's now-dead anchor to the new §1.3.

Verification
- New tests/test-tp-runner-list.sh (15 assertions): populated / empty /
  unreachable, the substring-vs-anchored prefix trap, the blank-line trap
  (asserted by byte count, since $() strips a lone newline), one-line stderr,
  no per-launch input required, both prefix spellings, and an agreement check
  that `list` and apl_live_agent_names return the same names for the same
  stubbed fleet.
- tests/test-pump-lib.sh +5 assertions for the strict/tolerant split.
- Mutation-checked both traps: making the printf unconditional fails the
  blank-line assertion; swapping strict for tolerant fails three.
- Full suite green (13 suites) except one pre-existing host artifact — see
  below. The pump does NOT call `list` yet, by design (G3.2).

Two environment findings, neither in this task's scope
1. The agent's own launch environment breaks the test suite. The pump exports
   TASKPUMP_TASKS_DIR/TP_TASKS_DIR into the container, and config.sh's
   "canonical env beats legacy" rule then outranks the fixtures' own
   ARACHNE_TASKS_DIR, so suites plan against the real ledger: 56 failures in
   test-tp-pump, 5 in test-golden-plan, 3 in test-conf-hermeticity, all
   spurious. Any dogfooding agent that runs ./tests/run-all.sh sees a red
   baseline it did not cause. Worth a task.
2. test-entrypoint's one remaining failure is a host artifact: the fixture
   copies the real ~/.claude and hits Permission denied on root-owned plugin
   pack files. Unrelated to this change; failed identically at baseline.
   Separately, a suite writes <sha>/info/exclude directories into the repo
   root on every run (worktree-visibility work from G0.6/G0.7) — untracked
   litter, removed by hand here.
