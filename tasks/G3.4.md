---
id: "G3.4"
phase: "G3"
title: "Reject branch names that cannot round-trip the slug"
status: done
claimed_by: null
claimed_at: "2026-08-13T23:19:34Z"
turn_budget_remaining: null
consecutive_failed_iterations: 0
blockers:
  - "G2.3"
completed_by_commits:
  - 1c397e8
milestone: "v0.2.0"
files:
  - "libexec/tp-task"
  - "libexec/tp-pump"
  - "docs/RUNNERS.md"
last_heartbeat_head_sha: null
completed_at: "2026-08-13T23:36:43Z"
goal: "A branch whose name cannot round-trip the container-name slug is rejected at claim and launch time with a clear message, instead of confusing liveness enumeration later."
last_heartbeat_ts: "2026-08-13T23:21:31Z"
blocked_at: null
blocked_reason: null
resume_attempts: 0
resume_head_sha: null
---

# G3.4 — Reject branch names that cannot round-trip the slug

## Spec

The slug map (`/` → `-`) is lossy for branches with more than one `/`, which
breaks the name→branch reverse mapping liveness and resume depend on
(RUNNERS.md §2). Close it by validation, not by a cleverer encoding:

- `tp task claim --branch` rejects a branch whose name contains more than one
  `/` (or any character the slug cannot represent distinctly), with a message
  stating the constraint and the reason (the container-name round-trip).
- The pump's own branch construction (`TASKPUMP_BRANCH_PREFIX` + phase) is
  validated once at startup so a misconfigured prefix fails the run loudly at
  tick zero, not per-launch.
- RUNNERS.md §2 documents the constraint as enforced rather than advisory.

## Scope

Validation and docs. Do NOT change the slug encoding itself — existing
container names on operators' hosts must keep matching.

## Acceptance

- Claim with `feat/a/b` fails with the constraint message; `feat/a` passes
  (asserted in the task suite).
- Pump startup with `TASKPUMP_BRANCH_PREFIX=x/y/` exits with a config error
  naming the key.
- CHANGELOG notes the previously-silent breakage now fails loudly.

## Completion notes (2026-08-13T23:36:43Z)

Branch names that cannot round-trip the slug are now refused.

What landed
- lib/pump-lib.sh: apl_branch_slug_reject_reason — the single rule, next to
  apl_agent_prefix (the other half of the same join key). Rejects >1 "/", a
  leading "/", a trailing "/", whitespace, and empty. Everything else is
  accepted, including branches with "-" (feat/f79-x) and with no separator at
  all (main) — the encoding is frozen, so this is a gate, not a re-encoding.
- libexec/tp-task: cmd_claim validates --branch before touching the file. The
  message names the ambiguous slug it would have produced and states the rule.
- libexec/tp-pump: validates the branch the run WOULD construct, once, after the
  phase-range check and before any tick, naming TASKPUMP_BRANCH_PREFIX /
  --branch-prefix. The prefix applies to every phase in the range, so it is a
  configuration error and belongs at tick zero.
- docs/RUNNERS.md §2: the constraint is now "enforced, not advisory", with a
  table of the two doors and a note that the encoding is frozen.
- CHANGELOG Unreleased: notes that the previously-silent breakage now fails loudly.

Verification
- test-pump-lib.sh +12: the rule directly, accepted and rejected shapes, and that
  the reason carries the collision rather than just saying no.
- test-tp-task.sh +12 (Tests 200 total): claim feat/a/b refused with nothing
  written and the task left open; feat/a and main accepted; /leading, trailing/
  and "has space" refused.
- test-tp-pump.sh +6 (Test 30c): TASKPUMP_BRANCH_PREFIX=x/y/ aborts non-zero,
  names the key, shows x-y-f55, and prints no plan; the default feat/ prefix and
  a separator-free prefix both still work.
- Full suite green (14 suites) except the pre-existing test-entrypoint host
  artifact.

One thing this surfaced
tp-task now sources lib/pump-lib.sh (it previously sourced only lib/config.sh).
The workspace-resolution fixtures in test-tp-task.sh describe themselves as "a
self-contained TaskPump install" but vendored the CLI plus one hand-picked lib
file, so they broke; they now copy lib/ wholesale, which is what that comment
already claimed. A real vendored workspace is a full git worktree and always had
the file. If TaskPump ever supports vendoring a hand-picked subset of files, that
subset now includes lib/pump-lib.sh.
