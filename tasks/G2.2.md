---
id: "G2.2"
phase: "G2"
title: "Full suite and shellcheck green on the flipped tree"
status: done
claimed_by: null
claimed_at: "2026-08-13T06:44:16Z"
turn_budget_remaining: null
consecutive_failed_iterations: 0
blockers:
  - "G2.1"
completed_by_commits:
  - c74de88
  - 3227bef
milestone: "v0.1.0"
files:
  - "tests/run-all.sh"
  - ".github/workflows/ci.yml"
last_heartbeat_head_sha: null
completed_at: "2026-08-13T06:58:21Z"
goal: "Every suite in tests/run-all.sh and the shellcheck -S error job are green on the flipped tree, locally and in CI."
last_heartbeat_ts: null
blocked_at: null
blocked_reason: null
resume_attempts: 0
resume_head_sha: null
scrub_reason: null
---

# G2.2 — Full suite and shellcheck green on the flipped tree

## Spec

The release gate for the flip work as a whole. Run `./tests/run-all.sh` and the
CI workflow on the assembled tree (all of G1 merged) rather than per-flip, since
the flips touch overlapping files and each was validated only against its own
suite slice. Shellcheck stays pinned at `-S error` with zero findings; if the
G1 work happened to reduce the pre-existing `-S warning` count (38 at
extraction), consider ratcheting CI to warning — a ratchet is in-scope, a
cleanup crusade is not.

## Scope

Test and CI runs, plus any small breakage they surface. New functionality found
missing here becomes a new task, not scope creep in this one.

## Acceptance

- `./tests/run-all.sh` summary table: every suite green, locally.
- The GitHub Actions run on the tip commit is green; its URL is in this task's
  completion notes.
- `shellcheck -S error`: zero findings.

## Blocked (2026-08-13T06:30:15Z)

Local halves green: run-all.sh 12 suites / 1060 tests passed on the assembled tree; shellcheck -S error zero findings (via koalaman/shellcheck:stable docker, CI's exact file list and flags); -S warning count is 59, UP from 38 at extraction, so no ratchet. Remaining criterion needs a human push: origin/main is ~95 commits behind, and pushing publishes the whole drain — reserved to the operator like G2.3's tag+push. Reopen after pushing; the CI run URL goes in the completion notes.

## Reopened (2026-08-13T06:44:09Z, from blocked)

push landed; CI run 31674526813 is RED (3 suites): NO_LAUNCH ticks die on missing ~/.claude in CI, and the placeholder-set goldens are locale-collation-dependent. Fixing both under this task's own scope.

## Completion notes (2026-08-13T06:58:21Z)

CI green on the flipped tree: https://github.com/tjmisko/TaskPump/actions/runs/31675580569 (shellcheck + tests jobs, main @ 3227bef). Local: all 12 suites / 1,060 tests green in the normal env AND under a CI-emulated HOME (no ~/.claude, C.UTF-8); shellcheck -S error zero findings via koalaman/shellcheck:stable docker on CI's exact file list. Two defects fixed under this task after the first CI run (31674526813) came back red: (1) tp-pump required ~/.claude on NO_LAUNCH ticks — check moved inside the launch-prerequisite guard (c74de88); (2) placeholder-set goldens were locale-collation-dependent — LC_ALL=C pinned and goldens re-recorded in byte order (3227bef). -S warning census is 59, catalogued in G2.4; the ratchet flip happens there.
