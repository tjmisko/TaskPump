# Generated by `tp task next`, which the README tells you to run. It is the JSON
# hand-off surface (TASKPUMP_TASK_OUT), regenerated on every call — state, not
# fixture content, so exercising the docs must not dirty the tree.
.next-task
