# Session Note — 2026-08-13 00:25 — g2-close-v0.1.0

## Completed
- **G2.2**: operator push surfaced a RED first CI run (31674526813). Two real
  defects fixed: tp-pump demanded `~/.claude` on NO_LAUNCH ticks (57 failures
  across tp-pump/conf-hermeticity in CI; check moved into the launch-prereq
  guard, c74de88); placeholder-set goldens were locale-collation-dependent
  (LC_ALL=C pinned + re-recorded in byte order, 3227bef). Green run:
  actions/runs/31675580569.
- **G2.4** (authored + executed this session): full 59-warning census bucketed
  A–H in the spec; cleared to ZERO in ea48b94/28e30a2/b00fef6; ci.yml ratchet
  flipped to `-S warning`. Green ratcheted run: actions/runs/31676859087.
  Notable: SC2318 in tp-monitor was a real latent bug (demo_sessions' `cut`
  computed from the caller's scope); tp-agent-watchdog REPO_ROOT and
  tp-monitor `ceiling` were dead.
- **G2.3**: v0.1.0 released — VERSION 0.1.0, CHANGELOG dated, annotated tag on
  0fbf96f, pushed. Arachne F90.4 notified (ops commit eb1fc29, **not pushed** —
  operator's call).

## In Progress
Nothing claimed.

## Decisions Made
- Operator delegated the pushes this session ("run the pushes"); tag cut after
  the warning zero per their sequencing.
- G2.4 landed as 3 commits, not per-bucket (buckets interleave within files;
  hunk staging unavailable) — deviation recorded in completion notes.

## Blockers / Open Questions
- Arachne ops repo has the F90.4 note committed locally but unpushed.
- G0.2 (dogfood launch conf) still open, hand-run.

## Recommended Next Session
Frontier is open: G3.1/G3.4/G3.5/G3.6/G4.1/G4.2/G5.2 eligible (cap 4);
hold G4.3 until G3.1 lands (both rewrite runner.sh/RUNNERS.md); mind G1.6's
flag (`$TASK_CLI claim` under `|| true` no-ops with a bare `tp` on PATH).
Consider running the fanout THROUGH the pump as G0.2's live validation.
