# Session Note — 2026-08-12 23:30 — g-drain-waves-2-5

## Completed
- **G0.3** (rescued the dead agent's uncommitted worktree): `TASKPUMP_NO_CONF=1`
  off-switch; hang diagnosed as the leaked `TASKPUMP_BUILD_GATE='./tests/run-all.sh'`
  recursing through gate-less pump ticks — not the TASKS_DIR suspicion.
- **G1.2**: golden suite re-anchored via `TASKPUMP_CONFIG` pins file
  (sentinel prelude + arachne.conf) + FLIP_CENSUS presence check.
- **G1.3–G1.7** (parallel agents, serial integration with rebases): filenames
  `.taskpump-*` + new `TASKPUMP_LOCK_NAME`; identity via new
  `TASKPUMP_PUMP_PROG_NAME` (goldens kept arachne under pins); `tp-agent-`
  prefix, image REQUIRED, entrypoint `/entrypoint.sh`; probe `tasks/` + `^T`
  grammar + discovered-conf anchoring (new mechanism, 6d53c79); verify-cmds
  empty via renderer `{{#KEY}}` sections, reclaim opt-in, net-health opt-out
  (goldens +1 `gates:` line each, supervisor-verified).
- **G1.8**, **G2.1** (README rewrite landed w/ typo fix; primary dirt verified
  subsumed, stash dropped). Supervisor extras: `.gitignore` `.taskpump-*`
  patterns (4b523b5), minimal.conf reframe (0438cd0), **G3.6 authored** —
  bare repo w/o `tasks/` silently falls back to the INSTALL ROOT's ledger
  (G2.1's agent wrote into our dogfood ledger, then hard-reset).
- **G2.2 local halves**: suite 12/12 (1,060 tests) on assembled tree;
  shellcheck `-S error` clean (docker koalaman/shellcheck:stable).
- Housekeeping: 10 merged agent worktrees removed; stray fork branches deleted.

## In Progress
- **G2.2** blocked: needs `git push` (origin/main ~95 behind), then the green
  Actions URL in completion notes. `-S warning` is 59 (up from 38) — no ratchet.

## Decisions Made
- Pushing anything (G2.2's CI check, G2.3's tag) is operator-only.
- G3.5 gate degradation deliberately NOT in Still-Arachne-shaped list (spec's
  exact-two-items instruction); discrepancy recorded in G2.1 notes.

## Blockers / Open Questions
- Drain stalled by design: everything left is blocked on G2.3 (hand tag+push)
  or is hand-run (G0.2). Frontier for agents is empty.

## Recommended Next Session
Operator: push main, reopen+complete G2.2 with the CI URL, then G2.3
(tag v0.1.0 + push). Then waves 6–7 (G3.x/G4.x, cap 4) fan out; G4.3 waits
for G3.1 despite eligible blockers (both rewrite runner.sh/RUNNERS.md);
mind G1.6's G4.3 flag: `$TASK_CLI claim` under `|| true` no-ops with a bare
`tp` on PATH.
