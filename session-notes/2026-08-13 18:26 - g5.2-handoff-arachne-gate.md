# Handoff — G5.2, the Arachne cutover gate

**Written:** 2026-08-13 18:26 PDT
**For:** a fresh agent working in `~/Projects/TaskPump`
**Written by:** an Arachne-side session; I did NOT claim anything and made no
writes to this repo except this file.

---

## Your first deliverable is a PLAN, not a patch

Read this note, verify its claims yourself (they are dated and this repo moved
four times today), then **write a plan** for what you will do and in what order.
Get it agreed before you edit anything. The reason is not ceremony: several
files here are owned by in-flight PRs and freshly-specced tasks, and the cost of
touching one is a merge conflict inside a repo that Arachne's entire F80–F90
backlog is currently gated on.

---

## State as of this note

- **HEAD `08c9e4e`**, branch `main`, up to date with `origin` (0 behind).
- **12 suites green: 1,088 assertions**, 0 failures. Verified by running
  `./tests/run-all.sh` at `66476c4` with pump env scrubbed (see caution 3).
- **No pump supervisor is running.** No process, no systemd unit, no container.
  `docker ps -a` shows only an unrelated `jobfeed-ollama`.
- **`.taskpump-pump.state` is stale and says otherwise** — `status: running`,
  `live_agents: 1`, `open_tasks: 6`, from a single tick at `21:46:49Z`. Its
  `open_tasks` still counts the reverted `T1`. **Do not trust it**; that
  dishonesty is exactly what task **G4.6** (issue #22) exists to fix, and G4.6
  is not yours (see the boundary below).

**Open tasks:** G4.1, G4.2, G4.3, G4.4, G4.5, G4.6, G5.2. Everything G0–G3 and
G5.1 is `done`.

**In flight:** PR #16 (draft — keep `GITHUB_TOKEN` out of process argv, issue
#14), PR #13 (G3 runner contract v2 — needs a fix pass). Merged today: #19
(`setpriv` not `su`, closing #15), #21 (suite git-stub litter, closing #17/#20).

---

## What you may touch

### G5.2 — do this first. It is the gate.

`Re-verify the F80 dry-run equivalence against tagged v0.1.0`. Open, unclaimed,
eligible (its only blocker G2.3 is done), milestone `arachne-f90`. Its work is
**read-only against the tools** — it touches no file any open PR or new task
owns.

It is the gate in a literal sense: its acceptance says the result must be
recorded *"before Arachne's F90.5 canary runs,"* and F90.5 gates every
pump-driven drain of Arachne's F82–F89 backlog. Nothing downstream moves until
this is done.

### G4.1 — only if you still have room after G5.2

`tp init scaffolds a new consumer`. Eligible, and its main artifact is a **new**
`libexec/tp-init`, so it collides with nothing in flight. Do not start it until
G5.2 is recorded.

## What you may NOT touch

| Off limits | Owned by |
|---|---|
| `tests/run-all.sh`, suite prologues | **G4.5** (suite env hermeticity, #18) |
| `libexec/tp-pump`, `libexec/tp-monitor` | **G4.6** (pump state honesty, #22) |
| `runners/claude-docker/*`, `tests/test-entrypoint.sh` | **draft PR #16** |
| anything on `feat/g3` | **PR #13** (needs a fix pass) |

If G5.2's work seems to require a change in any of the above, **stop and report
it** rather than making it. That signal is more valuable than the fix, and the
fix belongs to whoever owns the file.

---

## Three cautions — all three have already cost someone time today

1. **Pull `main` first, and record the SHA in every capture.** `main` moved
   three times today (#19, #21, and the G4.5/G4.6 ledger commits). A capture
   without a SHA is not evidence — G5.2's whole output is a comparison record,
   and a record that cannot be re-derived is worthless.

2. **Run ledger mutations (`tp task claim G5.2 …`, `heartbeat`, `complete`) from
   the PRIMARY checkout, never from a worktree.** The conf self-locates the
   ledger from `$PWD`. This bit the maintainer today and cost a cherry-pick of
   two stray ledger commits off a fix branch. This is the same failure class as
   Arachne's 2026-08-01 wrong-ledger incident, arriving through a new door.

3. **Keep scrubbing pump env when running suites** — `env -u TASKPUMP_CONFIG -u
   TASKPUMP_TASKS_DIR -u TASKPUMP_LEDGER_REPO …` — until **G4.5** lands. Issue
   #18 is tasked but not fixed: a pump-exported ledger env outranks the suite
   fixtures and produces **64 spurious failures**. That is a false RED. If you
   see a large failure count, check your environment before you believe it.

---

## What G5.2 requires, and what is already done

Read `tasks/G5.2.md` for the authoritative spec. Its shape:

- In the **Arachne** checkout, with `tp` resolved from **tagged `v0.1.0`** and
  the **full** `examples/arachne.conf` (not the six-line subset) as
  `taskpump.conf`, capture:
  1. `tp task ready --phases F80` (table)
  2. the same with `--json`
  3. `tp pump --phases F80 --dry-run`
- Byte-diff each against the `scripts/arachne-*` equivalent at Arachne's HEAD.
- Triage every diff to a named G1 default flip; accept it or fix it.
- Record the result: append to the thread of
  `ops/planning/handoffs/2026-08-12 19:17 - f80-via-taskpump-rebinding.md`
  **in Arachne's ops repo** (confirmed present), and comment Arachne's F90.4 and
  F90.5 task bodies with the outcome.

### Already verified — do not redo, but do re-run at your SHA

Capture 1 is done in both directions and was re-confirmed against current `main`
after today's merges:

- From the **Arachne repo root**: `tp task ready --phases F80..F90` is
  **byte-identical** to `scripts/arachne-task ready --phases F80..F90`.
- From a **subdirectory** (`crates/arachne-schema`): still byte-identical —
  *when driven by the derived Arachne conf described below.*

### The finding that will complicate your acceptance — read before capturing

**G5.2 as written verifies the upstream `examples/arachne.conf`, and that file
fails from a subdirectory.** Reproduced today:

```
cd <arachne>/crates/arachne-schema
TASKPUMP_CONFIG=~/Projects/TaskPump/examples/arachne.conf tp task ready --phases F80..F90
# -> header row only. Empty frontier. rc=0. Against ten open tasks.
```

That is issue **#1** (relative conf paths resolve against `$PWD`), and it is
worse than the issue text: no error, no warning, exit success. It is the same
false-drain shape as the 563-tick idle incident.

So G5.2's acceptance — *"byte-identical for all three captures"* — holds from the
repo root and **fails from anywhere else**. Your plan has to say how you handle
that. Options, and I am not choosing for you:

- Capture from the repo root (where it passes), and record the subdirectory
  failure separately as a live #1 reproduction with the command above. Honest,
  minimal, and does not expand scope.
- Argue that what F90.4 will *actually* pin is not `examples/arachne.conf` but a
  **derived** conf, and verify that instead — with the divergence documented.

A drafted derived conf already exists and is worth reading either way:
**`/tmp/claude-1000/-home-tjmisko-Projects-Arachne/61385129-545f-44ca-a11f-f56ac83462b3/scratchpad/taskpump.conf`**
(Arachne-side scratchpad; copy it somewhere durable if you use it). It is
`bash -n` clean, 54 keys, and diverges from `examples/arachne.conf` in five ways:

1. `TASKPUMP_TASKS_DIR` and `TASKPUMP_LEDGER_REPO` **dropped** (#1) — the fix for
   the failure above. `TASKPUMP_LEDGER_PROBE` alone suffices: `tp-task:80`
   derives the tasks dir from the resolved anchor, and `tp-pump:83-86` falls back
   to `$REPO_ROOT/ops/task-loop/tasks`, which is Arachne's own layout — the
   reason #2 does not bite this consumer.
2. `TASKPUMP_CODE_REPO` **dropped** — not in F90.4; same mechanism as #1 applied
   to one more key. `git -C .` walks up so it survives a subdirectory, but from
   inside a vendored `taskpump/` submodule it resolves to the *submodule's* root,
   and the heartbeat productivity check then measures the wrong repo — disarming
   the consecutive-failure tripwire.
3. `TASKPUMP_SUBMODULE_PROBE=taskpump/bin/tp` **added** (#3).
4. `TASKPUMP_JOBS=4`, not 6 (#8).
5. `TASKPUMP_PRE_FLIGHT` **dropped** (#5), taking F90.4's option (a).

It also carries **all 13 of #4's mislabeled pins re-annotated against v0.1.0
source with file:line**, which is directly reusable if you touch #4.

---

## Suggested plan shape (yours to revise)

1. `git -C ~/Projects/TaskPump pull --ff-only`; record HEAD SHA and Arachne's
   HEAD SHA.
2. `tp task claim G5.2 --branch <b> --turns N` — **from the primary checkout.**
3. Resolve `tp` from tagged `v0.1.0` (not `main` — the spec says the tag, and
   the tag is what F90.4 pins). Note that today's merges are *not* in `v0.1.0`.
4. Take all three captures from the Arachne repo root; byte-diff each.
5. Take capture 1 again from a subdirectory; record the #1 reproduction.
6. Triage every diff to a named G1 flip. Any diff you cannot name is a finding,
   not a nit.
7. Append the record to Arachne's handoff thread; comment F90.4/F90.5 bodies.
   Arachne's frontmatter is CLI-owned — **body edits only**, and commit them
   before any `arachne-task` verb runs, because those verbs `git add -A` the
   whole tasks dir and your edit would land under the CLI's auto-message.
8. `tp task complete G5.2` — from the primary checkout.

---

## Housekeeping you may do, and one thing to know about it

The repo root currently has nine stray untracked dirs — `9999aaa`, `aaaa111`,
`aaaaaaa1111`, `bbbb222`, `cccc333`, `dddd444`, `eeee555`, `ffff666`, `info` —
each containing only `info/exclude`. **These are stale**: I generated them by
running the suite at `66476c4`, *before* #21 merged the fix. They are pure test
output and safe to delete:

```
rm -rf 9999aaa aaaa111 aaaaaaa1111 bbbb222 cccc333 dddd444 eeee555 ffff666 info
```

Worth confirming as you go: after #21, a fresh `./tests/run-all.sh` from the
repo root should leave `git status --porcelain` showing only `?? .worktrees/`.
If the dirs come back, #21 did not fully close #20 — report that, do not fix it
(`tests/run-all.sh` and the suite prologues belong to G4.5).

---

## Why this matters beyond TaskPump

Arachne's F80–F90 epic — roughly 130k LOC of planned deletion across six phases
— cannot start a pump-driven drain until F90.5's canary passes, and F90.5 cannot
run until F90.4's cutover lands, and F90.4's config correctness is what G5.2
verifies. Four of the nine v0.1.0 issues (#1, #3, #4, #6) are config-resolution
bugs whose shared failure mode is **silent** — an empty frontier, a false drain,
a wrong ledger. G5.2 is the last cheap chance to catch a bad pin before it is
underneath a multi-day unattended run.
