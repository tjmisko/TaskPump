# Session Note — 2026-08-12 20:59 — g-fanout-wave-1

## Completed
- Seeded the G ledger (24 tasks, phases G0–G5) with full Spec/Scope/Acceptance
  bodies; `taskpump.conf` dogfood config; drain runbook + session fanout
  protocol in `docs/design/`.
- Wave 1 via in-session agents (pump guards replicated by hand): **G0.1**
  (tp-pump honors `TASKPUMP_PHASE_SIGIL`, malformed ranges abort up front —
  merged, pump suite 224/224 confless), **G1.1** (29 flip-census keys pinned
  in `examples/arachne.conf` — merged), **G5.1** (`docs/design/f90-addenda.md`,
  create-ready F90.8/F90.9 for Arachne's ops — merged).

## In Progress
- **G0.3** (claimed, `feat/g0-3`, agent running): suites leak the enclosing
  repo's `taskpump.conf` via config discovery — 37 sigil-shaped pump-suite
  failures from the primary, plus full-suite runs that hang in `test-tp-pump`
  (suspect `TASKS_DIR=tasks` pointing fixtures at the real ledger). Fix is a
  first-class conf off-switch exported by `run-all.sh`. Agent paused awaiting
  its own full-suite verification.

## Decisions Made
- Supervisor owns all ledger writes; agents work confless worktrees, never push.
- Harness worktrees fork from session-start HEAD — every future agent must
  `git switch -c <branch> main` first.
- G1.2 held until G0.3 merges: no re-anchoring suites on a leaking harness.
- G0.2 stays hand-run (needs a live container `pump --once`).

## Blockers / Open Questions
- Primary-context gate is red/hanging until G0.3 lands; merges since G0.1 were
  gated by inspection (suite-inert diffs only).

## Recommended Next Session
Collect agent-g0-3's report, merge behind a full primary-context `run-all.sh`,
complete G0.3, then launch G1.2 (branch from main). Wave 3 (G1.3–G1.7) follows;
specs already carry G1.1's corrections.
